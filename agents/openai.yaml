interface:
  display_name: "Stellar WebMCP Trust Router"
  short_description: "Safe WebMCP tool routing with receipts"
  default_prompt: "Use $stellar-webmcp-trust-router to inspect a WebMCP-enabled page, classify tool risk, require confirmation, execute approved tools, and produce an auditable receipt."
